<div class="box-card shadow-lg">
  <%= render "template_badge", camp: camp %>
  <h1><%= camp.name %></h1>
  <div class="d-flex justify-content-center">
    <h3 class="pt-3"><i class="bi bi-person"></i> <strong>HLL:</strong> <%= camp.leader.pfadiname %></h3>
  </div>
  <hr>
  <h3 class="text-left">Allg. Beschreibung: </h3>
  <%= camp.general_description %>
  <hr>
  <% if camp.template != "Kein Lager ausgeschrieben" %>
    <h3 class="text=left">Diesjährige Beschreibung: </h3>
    <%= camp.this_camp_description %>
    <hr>
  <% end %>
  <div class="d-flex justify-content-between">
    <%= link_to "Bearbeiten", edit_camp_path(camp.id), class: "btn btn-custom-success m-3" %>
    <%= link_to "Zur Lagerseite", pfila_path, class: "btn btn-custom-primary m-3" %>
  </div>
</div>
