<% content_for :title, "Kleiders" %>


<h1 class="dm-sans-title" style="text-align: center; font-size: 80px">Kleiderkasten</h1>
<p class="dm-sans-description" style="text-align: center; font-size: 20px">Der Kleiderkasten bietet Ihnen alle Pfadikleider an.</p>

<div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3 m-3" id="kleiders">
  <% @kleiders.each do |kleider| %>
    <%= render kleider %>
  <% end %>
</div>

<% if current_leader.present? %>
  <%= link_to "Neues Kleidungsstück", new_kleider_path, class: "btn btn-primary btn-lg", style: "margin: 20px" %>
<% end %>

<% @bestellung = Bestellung.new %>
<%= render partial: "bestellung/form" %>
