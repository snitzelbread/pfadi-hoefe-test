<!DOCTYPE html>
<html lang="de">
<head>
  <title><%= content_for(:title) || "Pfadi Höfe" %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= yield :head %>

  <%= favicon_link_tag("logo/pfadi_logo.jpeg") %>
  <link rel="apple-touch-icon" href="logo/pfadi_logo.jpeg">


  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Playpen+Sans:wght@100..800&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,100..1000;1,9..40,100..1000&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Fuzzy+Bubbles:wght@400;700&display=swap" rel="stylesheet">

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>

</head>

<body style="background-color: hsl(30, 50%, 98%)">
<% if controller.controller_name === "dashboard" %>
  <div class="container-fluid">
    <%= render partial: "shared/alerts" %>
    <%= yield %>
  </div>
<% else %>

  <%= render partial: "shared/navbar" %>
  <div class="container-fluid">
    <div class="container">
      <%= render partial: "shared/alerts" %>
      <%= yield %>
    </div>
  </div>
<% end %>

<dialog id="turbo-confirm">
  <p>Bist du dir sicher?</p>
    <form method="dialog">
      <div class="d-flex gap-3">
        <button class="btn btn-custom-primary-sm" value="cancel">Abbrechen</button>
        <button class="btn btn-custom-danger-sm" value="confirm">Ja, ich bin mir sicher</button>
      </div>
    </form>
</dialog>

<%= render partial: "shared/footer" %>
</body>
</html>
