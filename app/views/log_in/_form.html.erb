<%= form_with model: @leiter, url: login_path do |form| %>
  <% unless leiter.nil? %>
      <% if leiter.errors.any? %>
        <div style="color: red">
          <h2><%= pluralize(leiter.errors.count, "error") %> prohibited this Leiter from being saved:</h2>
          <ul>
            <% leiter.errors.each do |error| %>
              <li><%= error.full_message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>
    <% end %>
  <div class="mb-3">
    <%= form.label :pfadiname, style: "display: block", class: "form-label" %>
    <%= form.text_field :pfadiname, placeholder: "koala", class: "form-control" %>
  </div>
  <div class="mb-3">
    <%= form.label :password, style: "display: block", class: "form-label" %>
    <%= form.password_field :password, placeholder: "Passwort", class: "form-control"%>
  </div>
  <div class="mb-3 center" style="top: 0">
    <%= form.submit "Sign In", class: "btn btn-success"%>
  </div>
<%end%>